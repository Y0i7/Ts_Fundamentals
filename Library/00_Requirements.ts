/** 
 * Requisitos para TypeScript
 * 
 * TypeScript es un lenguaje de programación que no es entendido de forma nativa por los navegadores actuales.  
 * Por lo tanto, es necesario usar un archivo de configuración para compilar los archivos .ts en .js,  
 * permitiendo que los navegadores puedan leer nuestro código.  
 * 
 * En este caso, nuestro archivo de configuración se llama `tsconfig.json`. Puedes generar el tuyo  
 * desde la terminal usando el comando: `tsc --init`.  
 * 
 * Asegúrate de estar en el directorio correcto del repositorio antes de ejecutar el comando.  
 * Una vez ejecutado, se generará el archivo `tsconfig.json` con la configuración necesaria para tu proyecto.  
 */
